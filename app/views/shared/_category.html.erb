<div class="category-ui-card">

  <%= image_tag(asset_path("category_pictures/#{category.name.downcase}.svg"))%>

  <div class="link"><h5><%= link_to category.name, category_path(category) %></h5></div>

</div>
